import { StyleSheet } from 'react-native'
import { Colors } from '../../Themes/'

export const tintColor = Colors.snow;

export default StyleSheet.create({
  header: {
    backgroundColor: Colors.eggplant,
    borderBottomWidth: 0
  },
  text: {
    color: Colors.snow
  }
})
